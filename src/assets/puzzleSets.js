import polgar from './polgar.json';
import lichessMatingPatterns from './lichess_mating_patterns.json';

// Add new puzzle sets here. The first entry is the default.
const puzzleSets = [polgar, lichessMatingPatterns];

export default puzzleSets;
